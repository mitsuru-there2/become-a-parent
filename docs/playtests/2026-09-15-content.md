# 設定化・難易度・追加パックの検証

2026-09-15。対象：D-024 / S-015。新規 rules-2 / data-2 / save-3、操作cli-2。

## 結果

| 対象 | 根拠・確認内容 |
| --- | --- |
| AC-015-A | `tests/engine.test.ts`：標準・パックなしで従来5本の200期と成人後の状態・収支・結果が一致 |
| AC-015-B | `tests/content.test.ts`：3難易度で初期資金・費用が変わり、公開操作でパック付き40期・成人後まで完走・再生 |
| AC-015-C | 設定例の家庭・行動・イベントの追加、行動の費用・親Aの時間・能力効果・対象期間、確率0/100、再発間隔、追加抽選による既存抽選の不変性を検証 |
| AC-015-D | 不明な難易度・パック・行動、ID衝突、依存不足・循環、不正な確率・効果・参照画像ID・条件パス・特殊キーを拒否。行動の時期制限・時間超過・確定再送も検証 |
| AC-015-E | 設定更新・登録パック削除後も再生と公開状態が一致。書出し・取込が一致。変更前の実装から生成したsave-2 fixtureを再生。設定ハッシュ不一致は読込を拒否。開始要求の再送はパック登録削除後も元の応答を返す |
| AC-015-F | Chromeの公開UIで「むずかしい」を選択→40期→結末→再読込→書出し→別ブラウザコンテキストへの取込が成功。追加パックの選択・家庭選択・追加行動の保存と再読込・イベント画像を別途確認 |

`bun run check`（書式・lint・型）、`bun run check:content`、`bun run test`（31件）、`bun run build` を実行。ビルドは成功。出力JSの500kB超のサイズ警告は残る（約648kB、gzip約207kB）。配信性能の測定・分割最適化はこの検証には含めない。

CLI単体は `/tmp/parent-content-cli` に `hard` / home-01 / seed0で新規作成し、初期資金80・費用230の公開応答を確認。公開操作による難易度ごとの完走は共通Serviceを使う自動テストで行った。

## ブラウザ確認

- 環境：ローカル `http://127.0.0.1:5173`、Chrome headless、1280×900 / 390×844。
- Browser plugin not available。フロントエンド検証スキルの経路に従い、既存のPlaywrightスクリプト `scripts/browser-check.ts` を使用。
- コンソール検査で見つかったfavicon.icoの404は、未使用アイコンの自動取得を抑える指定をindex.htmlへ追加して解消。
- ページタイトル・開始操作・エラーオーバーレイなし・コンソールと実行時エラーなし・デスクトップとスマホの画像・横はみ出しなしを確認。
- 画像証跡：`/tmp/parent-browser-check/home.png`、`game-desktop.png`、`game-mobile.png`、`ending.png`、`dlc-desktop.png`、`dlc-mobile.png`。標準ブラウザ検証の結果は同ディレクトリの `result.json`。
- 追加パックの画面検証では、サンプルのイベントを確率100%・1期から・行動条件なしにし、画像IDをheroにした検証用設定を一時登録した。これは配置と操作を再現可能に確認するための設定で、バランス評価ではない。7期目で追加行動の選択・保存・再読込を確認し、公開DOMだけで判定した。検証後に `config/packs.json` を元の空配列へ復元した。

## 検証の区別と限界

エンジンの条件・効果・確率境界・旧保存整合性の検証は内部状態を参照するテスト。3難易度の完走とブラウザ操作は公開状態・選択肢だけで操作した定型方針の検証であり、独立LLMの自由プレイ、人の面白さ評価、難易度の適正さの検証ではない。数値はゲーム用の調整仮値。

DLCの商品内容・価格・決済・購入権利の検証・返金・有料データ配信は未実装。設定には画像の参照だけを保存するため、古い保存で利用した画像ファイルの配信継続は運用側で維持する。追加画像を制作した検証ではない。

## Valibot導入の検証（D-025）

2026-09-15。Valibot 1.5.0を固定して導入。構造・値域・必須項目・未知キーの検査を `src/content/schemas.ts` / `src/service/schemas.ts` へ移し、設定型を `InferOutput` から導出した。画像・行動の参照、パック依存、設定ハッシュは従来の共通処理で検査する。ルール・データ・保存・公開操作の版は維持する。

- `bun run check`：書式・lint・型検査が成功。
- `bun run check:content`：本編設定と画像参照の検査が成功。
- `bun run test`：既存31件＋追加5件、計36件成功。標準200期・成人後、3難易度、DLC、旧保存と再生の回帰を含む。
- `tests/validation.test.ts`：不正な入れ子・未知キー・文字列数値・NaN・Infinity・特殊キー・空の部分編集を拒否し、入力を補正・変更しないこととエラーの項目パスを確認。
- 同テストで実際のTanStack Routerとメモリ履歴を使い、Valibotスキーマを `validateSearch` に直接指定してルート照合。正しい値・省略時の既定値・不正値の `SearchParamError` を確認。追加アダプターは不要。アプリ本体に新たな検索パラメーターは追加していない。
- `bun run test:browser`：Chrome、1280×900 / 390×844で「むずかしい」の40期・成人後・再読込・書出し・取込が成功。コンソール・実行時エラー0。出力は `/tmp/parent-browser-check/result.json`。
- `bun run build`：成功。導入前のJS約648.08kB（gzip207.41kB）から約655.23kB（gzip209.59kB）へ変化。gzip差分は約2.18kB。この変更全体の差であり、ライブラリ単体のサイズ計測やZodとの実測比較ではない。既存の500kB超の警告は継続。

Zod 4とValibotの直接対応は[TanStack Router公式](https://tanstack.com/router/latest/docs/guide/search-params)、モジュール構成の選定理由は[Valibot公式](https://valibot.dev/guides/comparison/)を参照した。

## JSON編集支援の検証

2026-09-15。JSONへの型付け依頼に対応し、`@valibot/to-json-schema` 1.8.0でJSON Schemaを生成。`.vscode/settings.json`で本編・旧互換設定・パック配列・単体パックを関連付けた。生成ツールと検証用JSON言語サービスは開発依存で、ゲームの実行時依存には追加しない。

- `bun run schema:generate`：3種類のスキーマを生成。生成物とValibot定義の同期検査を `bun run check` に追加。
- `tests/json-schema.test.ts`：VS CodeのJSON言語サービスに実際のファイル関連付けと生成物を渡し、本編・旧設定・サンプル・パック配列をエラーなく診断。文字列の費用、101%の確率、未知キー、必須項目欠落、配列ではない登録一覧を検出。項目名と `study` / `craft` 等の列挙値の補完を確認。
- `bun run check`：生成物同期・書式・lint・型検査が成功。`bun run test`：既存36件＋JSON編集支援4件、計40件成功。
- `bun run check:content`とビルドでも既存データを確認。ゲーム設定JSONの値、設定ハッシュ、保存形式は変更していない。

JSON Schemaへ変換できない独自 `check` 関数は生成時に除外し、既存Valibotと参照整合性検査を最終判断に使う。他の変換失敗はエラーにする。エディタUI自体を操作した検証ではなく、同じJSON言語サービスの補完・診断APIによる検証。正式な変換元は[Valibotスキーマ](../../src/content/schemas.ts)、生成手順は[設定ガイド](../../config/README.md)。変換方式は[Valibot公式](https://valibot.dev/guides/json-schema/)を参照した。
