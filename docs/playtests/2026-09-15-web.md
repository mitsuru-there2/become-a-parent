# React / TypeScript移植の検証

2026-09-15。SPEC 2.0 / rules-1 / data-1 / cli-2 / save-2 / ending-1。macOS arm64、Bun 1.3.14、Node.js 24.14.0、VitePlus 0.3.2。D-023・S-014の移植を検証した。

## 内部検証

`bun run test`：エンジンとサービスの17テストが成功。既存の5パターン×40期＝200期について資金、両親の状態、子の内部状態、祖父母の状態、成人後の最終結果が `calculated-traces.json` に一致。

観察の閾値と公開情報の隔離、条件イベントの優先・再発間隔、遅延成功／失敗、珍事と収支上限、親の先死亡・値の固定、5結末の分類を確認。保存ではIndexedDB再開、同一要求再送、同ID異入力、同revision競合、無効入力、未回答・資源制限、トランザクション失敗、未知版・破損拒否、終了後更新拒否、再生・交換形式を検証した。

テスト：[engine.test.ts](../../tests/engine.test.ts)、[service.test.ts](../../tests/service.test.ts)。これは内部検証であり、通常プレイの選択判断と区別する。

## 公開CLIの比較

```sh
bun scripts/public-play.ts /tmp/parent-cli-comparison 3
```

5方針×seed0〜2＝15実行が40期と最期まで完走・再生一致。追加の結末到達2実行を含め、計17実行・680期を確認。EN-01〜EN-05すべてが合法な操作列で到達できた。追加2実行は目的の結末を狙った検証であり、自由な通常プレイとは区別する。

集計・run ID・条件・最終結果は[CLI結果JSON](2026-09-15-cli-results.json)。全入力・公開応答・理由・観察IDは一時出力ディレクトリのJSON Linesへ記録した。スクリプトはCLIの公開出力だけを読み、保存ファイルやエンジン内部を選択判断に使わない。独立LLMの自由プレイではなく、旧版と同じ決定的な比較方針である。

## 実ブラウザ

agent-browserでホーム・開始・回答・確定の表示を確認。続けて[ブラウザ検証スクリプト](../../scripts/browser-check.ts)をChromeで実行。操作判断は画面のDOM・ラベル・有効状態だけを使い、内部ストア・IndexedDB・エンジンを参照しない。

- 新規開始、未回答時の確定無効、回答保存、再読込後の回答復元。
- 方針の未保存時は確定無効、保存後に有効になる。
- 全40期を画面操作で通過し、老後8節目を含む全48件の記録と結末を表示。
- 終了後の再読み込みでも結末を復元。
- ブラウザから保存をダウンロードし、別のブラウザコンテキストに取り込んで同じ結末を確認。
- 1280×900と390×844で確認。狭い画面に横方向のはみ出しなし。ブラウザの未処理エラーなし。

一時成果物：`/tmp/parent-browser-check/` にhome/game-desktop/game-mobile/endingのPNG、result.json、browser-save.json。後者をCLIへ取り込んで `replay` し、40期の一致も確認した。これは公開UIプレイ後の交換・再現検証。

## ツール・配信

`bun run check` で書式・lint・型検査、`bun run build` でSPAビルド、`bun run deploy:check` でWorkers Static Assetsのdry-runを確認。TanStack Routerの直リンクはSPA配信設定で処理する。

さらに `wrangler dev --local` でWorkers配信を起動。`/play/direct-link-check` がビルド済みindex.htmlを返すこと、JS資産が元ファイルと一致することを確認し、ビルド済み版でも40期・老後・全48履歴・再開・書出し／取込のブラウザ検証が成功した。成果物は `/tmp/parent-production-browser/`。

Cloudflareの既存認証が期限切れでrefresh不能だったため、実際の公開は未実施。`bunx wrangler login` 後に `bun run deploy` で公開できる。公開URLと本番配信の実機確認は残る。

## 削除と残課題

Godotのプロジェクト・GDScript・エンジン実体と配布ZIP、Python CLI・保存・旧GUI・テスト・検証スクリプト、旧起動commandを削除。仕様、歴史的な検証記録、正式HERO・候補画像は保持した。ユーザーの旧SQLite保存と既存のローカル検証データはコード削除の対象にせず保持している。現行版への自動変換は未実装。

人による面白さ・操作負担の評価、実スマートフォン・Safari/Firefoxの確認、長期保存容量の評価、課金、アカウント同期、ストア配布、年代別イラストは今回未実施。Q-014のバランス仮説は変更していない。

## 可読性改善後の回帰検証

同日の追加依頼で、実行コードの一文字変数を意味のある名前へ変更し、コマンド・イベント効果・入力項目の分岐をswitchへ整理した。勤務条件を名前付きの項目にし、イベントのタプルにラベルを付けて利用側で分割代入した。期首状態からの計算、効果の適用順、整数除算、公開情報の境界、再送・保存の注意点にコメントを追加。Reactの方針編集・家族・履歴・結末を個別コンポーネントへ分離した。版と保存データのキーは維持する。

- `bun run check`：書式・lint・型検査が成功。
- `bun run test`：既存17テストが成功。テスト本体と参照値は変更していない。
- `bun run build`：成功。500kB超のJSチャンクに対するサイズ警告は残る。
- 変更前と変更後にそれぞれ `bun scripts/public-play.ts <出力先> 1` を実行。5基本方針と結末を狙う2条件、計7実行・280期が完走し、再生一致。
- 上記プレイ後の内部比較では、7件の保存全状態、全280件の確定記録とdigest、revisionが完全一致。673件の公開応答・終了コードも、実行ごとに生成するrequest-idだけを除いて一致した。選択判断には内部状態を使わず、比較はプレイ後に別途実施した。
- `GAME_URL=http://127.0.0.1:5174 BROWSER_ARTIFACTS=/tmp/parent-refactor-browser bun run test:browser`：Chromeで40期・成人後・48履歴、回答保存、編集案の保存、再読込、書き出しと別コンテキストへの取り込みが成功。1280×900と390×844を確認し、横はみ出し・未処理例外なし。表示部品を分離しても同じ公開操作を利用できる。

比較用の一時出力先は `/tmp/parent-refactor-before/` と `/tmp/parent-refactor-after/`。ブラウザ証跡は `/tmp/parent-refactor-browser/`。Browserスキルが利用可能一覧にないため、既存のPlaywrightスクリプトを使用した。今回の変更でSafari/Firefoxや公開配信の再検証は行っていない。
