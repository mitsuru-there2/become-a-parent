# 開発計画

作成日：2026-09-12。更新日：2026-09-15。現在地：D-023に基づきTypeScriptエンジン・共通CLI・Reactブラウザ版へ移植。実装と検証は[移植記録](playtests/2026-09-15-web.md)。独立LLMの自由プレイと人の体験評価は未実施。

## 合意した開発順序

**詳細仕様を作成 → CLIで実装・検証 → 必要なUI・ビジュアルを追加**を、今後のゲーム開発・機能拡張でも繰り返す（D-021）。GUIはCLIのインターフェースとして扱う。全体の段階はテキスト版PoC → 操作GUI → 画像・演出 → バリエーション追加。最初から0〜20歳の育児と老後・最期までを扱い、イベント・初期条件・進路などの種類を少数に絞ってPoCを作る。

案2のコメディと、ボードゲーム的な偶然を楽しむ方針を採用済み。育児編は半年単位・全40ターンで確定済み。子どもはまず1人。少数のシンプルなパターンで、パラメータとイベントがどうつながり、選択が長期的な幸福にどう影響するかを確認する。将来の全バリエーションを先に定義する必要はないが、PoCに使うパターンは最期まで仕様をつなぐ。

工数は仕様と技術構成が決まった後に見積もる。段階は検証結果と完了条件で管理する。旧P0〜P4からの変更は意思決定記録D-005に残し、現行段階には意味が分かるIDを使用する。

## P-SPEC：基本パターンと詳細仕様を作る

目的：人生全体を通すテキスト版を実装可能な状態にする。

初版の詳細仕様・机上確認を作成後、2026-09-14のユーザー依頼でP-TEXTへ進んだ。現行版は[SPEC](SPEC.md)を参照。ユーザーの委任に基づく採用設計と、調整仮値を区別している。

P-SPEC内の順序、各ステップで決めること、進め方、成果物、完了条件、進捗は[仕様策定計画](specification-plan.md)を正本とする。SP-01〜09の成果と根拠は仕様策定計画を参照する。仕様番号順や未決事項番号順に決めるのではなく、依存関係に沿って進める。

成果物：パラメータ定義、イベント定義、接続条件、基本パターン、老後と評価、CLI契約、受け入れ条件とテストプレイ手順を備えた初版SPEC。

完了条件：SP-09の最終点検を満たし、少数の基本パターンが最期までつながり、追加のゲームルールを推測せずにCLIを実装できる状態である。詳細な進捗はこの文書に重複記載せず、仕様策定計画で更新する。

## P-TEXT：テキストベースのPoCを作る

目的：ビジュアルなしで、選択・変化・人生の振り返りがゲームとして成立するかを確認する。

範囲：子ども1人、0〜20歳の半年単位・全40ターンの進行、少数の基本パターンとイベント、家計・時間・ストレス・親子関係・子どもの状態、簡単な進路、老後の自動進行、最終評価と履歴、AIエージェントが操作できるCLI。SPEC S-001〜S-012が対象。

CLIを採用し、人向けのテキストとエージェント向けの構造化出力で操作できるようにする。具体的な契約はSPEC S-011で管理する。グラフィック、イラスト、アニメーションはこの段階に含めない。

後に回すもの：大量のイベント、詳細な職業や祖父母のバリエーション、複数の子ども、深刻な事件の多様な分岐、製品向けの演出・配布機能。

成果物：最初から最期まで遊べるCLI、操作方法、再現用の条件・操作記録、AIエージェントのテストプレイ記録と検証結果。

完了条件：

- 定義した基本パターンすべてで、育児から老後・最終評価まで通せる。
- AIエージェントが非対話のCLI操作と公開情報だけで完走でき、JSONの解析や選択肢の取得で表示文の推測に依存しない。
- 中断・再開と操作ログの再生で結果が一致し、無効入力や再送でターンや効果を二重適用しない。
- イベントがないターンも進行し、年代の境界と20歳での移行に欠落や二重処理がない。
- 同じ初期条件・乱数条件で異なる方針を比較でき、短期と長期の利点・負担を説明できる。
- 観察をもとに方針を変える場面と、悪化から回復する経路を確認できる。
- 最終評価が引き継いだ状態と履歴から計算され、結末の文章だけを差し替えたものになっていない。
- 異なる幸せと、社会的成功と幸福が一致しない結果を、採用した基本パターンで確認できる。
- 説明できない結果、単調さ、常に有利な方針を記録し、修正するか残す理由を明示する。

計算の整合性は必要な自動検証で確認する。AIエージェントは方針・シードを変えてテストプレイし、進行不能、偏り、説明できない結果を記録する。人による面白さや感情の評価は別に確認する。方法と比較条件はQ-007、記録の要件はSPEC S-012で定義する。

## P-VISUAL：操作GUIを付け、その後に画像・演出を追加する

目的：検証したゲームを、状態の把握と操作がしやすい画面にする。

2026-09-14の追加依頼（D-021）で、画像を作る前に情景テキストGUIを実装する範囲へ進んだ。操作仕様は[S-013](specs/gui.md)。正式HEROと今後のイラストの基準画像は採用済み（D-020）。画像・制作指針は[アートディレクション](art-direction.md)、残る描画・演出はQ-012を参照する。

範囲：家族の状態、今期の方針、出来事への対応、履歴、老後の振り返りを視覚的に表現する。画面や表現の仕様を先に追加し、テキスト版の計算とイベントデータを再利用する。

CLIを基本の開発・機能拡張・比較・再現の入口とし、GUIと将来のビジュアルはその公開操作に対応させる。

完了条件：

- テキスト版と同じ初期条件・乱数条件・選択で、同じ状態と最終評価になる。
- 必要な観察と選択肢が画面から利用でき、隠しパラメータが意図せず表示されない。
- 初めから最期まで画面操作で完走できる。
- 操作量と理解しやすさを確認し、問題を修正する。

この段階でゲームルールも変更する必要が出た場合は、表示の変更と区別し、SPECと共通の計算処理を更新して両方で確認する。

## P-EXPAND：バリエーションを徐々に増やす

範囲：イベント、子どもの個人差、家庭条件、仕事、祖父母の支援、進路、関係修復、結末を少しずつ増やす。複数の子どもも、この段階以降に独立した拡張として扱う。

追加ごとに、仕様 → CLIの共通計算・データ → CLIで再現・比較 → 必要なUI・ビジュアルへの反映 → 検証、を行う。

各追加の完了条件：

- 発生条件、状態変化、後続イベントや評価への接続を説明できる。
- 単に文章が違うだけでなく、観察や判断に新しい意味がある。
- 既存の基本パターンを再確認し、意図しない結果の変化がない。意図した変更は理由を残す。
- 人生全体を引き続き完走できる。

## P-RELEASE：製品として整える

バリエーション追加の後に、保存と互換性、難易度、対応環境、配布に必要な品質を整える。詳細は対象環境と提供範囲が決まってから仕様化する。

公開先は[ゲーム企画](game-concept.md#公開先と販売方式)のD-023に従う。追加セットの商品設計はQ-015、移植・提供方法はQ-016を、該当する実装の前に具体化してSPECへ反映する。公開先の方針決定だけで、移植・課金実装・ストア登録・公開の作業を開始しない。

旧ローカルGUIはReactのブラウザ版へ置き換え済み（D-023）。製品版の方式が決まっても、ゲームの機能拡張はCLIで実装・検証してから必要なUIを追加する。

完了条件：対応環境で一通り遊べ、保存・再開で進行が壊れず、採用した人数・イベント量でも操作が過大にならない。

## 検証記録

2026-09-14の実装・技術検証は[テキスト版の検証記録](playtests/2026-09-14.md)。Godotで全期間、5種類のエンディング、CLIと日本語対話、保存・再送・強制終了・再生を検証した。公開自動方針50実行が完走し、全件再生一致。これは独立LLMの自由プレイや人の面白さ評価ではない。

現在の成果物：[起動手順](../README.md)、[CLI操作ガイド](cli-guide.md)、[GUIの遊び方](gui-guide.md)、[技術構成](architecture.md)、[SPECの実装対応表](SPEC.md#実装検証の記録)。追加依頼により正式HEROを制作・採用し、[イラストの制作基準](art-direction.md)を記録した（D-020）。GUIの実装・確認は[GUI検証記録](playtests/2026-09-14-gui.md)。2026-09-15の移植で正式HEROを開始画面に利用し、Motionによる画面遷移を追加。年代別イラストは未制作。Q-014の調整仮説を残し、実装を利用した人の確認を次の判断材料にする。

以前の仕様の机上確認は下記に残す。

| 確認日・対象版       | 対象仕様・仮説                   | 基本パターン・初期条件・乱数条件 | 操作／方針               | 観察結果                                                                             | 次の判断                                |
| -------------------- | -------------------------------- | -------------------------------- | ------------------------ | ------------------------------------------------------------------------------------ | --------------------------------------- |
| 2026-09-13・SPEC 1.0 | S-001〜006・009・010の数式と接続 | BP-01〜05、seed0〜9              | 固定方針で内部の机上計算 | 全50計算が最期へ到達。詳細は[机上確認](specs/desk-check.md)。CLI・公開プレイは未検証 | 一括レビュー、Q-014の仮説をP-TEXTで確認 |

## 設定化・難易度・追加パック（2026-09-15）

D-024・S-015を実装。JSONでの調整、3段階の難易度、追加家庭・行動・イベントのパック、設定を固定するsave-3、旧save-2互換を追加した。操作・書式・検証は[設定ガイド](../config/README.md)、根拠は[検証記録](playtests/2026-09-15-content.md)。課金・購入権利・商品内容の確定は後続。難易度の数値は調整仮値で、人の面白さ評価は未実施。

D-025でValibotを導入し、設定型をスキーマから導出、設定・DLC・保存設定・CLI入力の検査へ適用した。[検証記録](playtests/2026-09-15-content.md#valibot導入の検証d-025)を参照。TanStack Routerとの互換性は結合テストで確認し、現在の画面のURL仕様は変更していない。

JSON編集支援として、ValibotからJSON Schemaを自動生成し、VS Code互換エディタへの関連付けと更新漏れ検査を追加した。JSON言語サービスで補完・診断を確認。[検証記録](playtests/2026-09-15-content.md#json編集支援の検証)を参照。

## フォームの統一（2026-09-15）

D-026・S-013に従い、新規開始・方針編集・取り込みをTanStack Formへ移した。入力値をNano Storesから分離し、フィールド単位の購読を実装。全46テスト、静的検査、ビルド、40期のブラウザ回帰検証を通過した。再描画と配信サイズを含む根拠は[フォーム検証記録](playtests/2026-09-15-forms.md)。

## 直近の進め方

Cloudflareの認証を更新して公開し、ブラウザ版を使った人の体験確認を行い、操作量とQ-014の調整仮説を検討する。ゲーム機能の変更は先にCLIへ実装・検証し、必要な画面を追随させる。画像・演出の制作は次の依頼とQ-012の具体化に合わせて進める。

## TanStack StartのSPAモードへの移行（2026-09-15）

D-027・[S-014 / AC-014-F](specs/web.md)を実装。`@tanstack/react-start` 1.168.54を固定し、既存のRouter 1.170.36（Startの依存と同一版）を継続使用する。構成と配信の正本は[技術構成](architecture.md)。公式Startスキル2種と付属資料を導入し、Formの確認結果・取得コミット・ライセンスは[スキル出典](../.agents/skills/README.md)に記録した。

検証済み：

- `bun run check`：書式・lint・型・JSON・設定検査成功。`bun run lint`も成功。取得したスキルの説明用サンプルと自動生成ルートをアプリのlint・format・pre-commit整形から除外した。
- `bun run test`：7ファイル47テスト成功。既存エンジン、CLIサービス、IndexedDB、フォーム購読、設定・JSON検査の回帰を確認。
- `bun run deploy:check`：Startが1ページのシェルを`dist/client/index.html`へ生成し、Wranglerが`dist/client`の10ファイルのみを配信対象として検査。公開デプロイは未実施。
- [公開UI検証スクリプト](../scripts/browser-check.ts)を`GAME_URL=http://127.0.0.1:5174`（Start dev）と`http://127.0.0.1:4174`（`wrangler dev --local`の静的配信）で実行。各40期から結末「それぞれの予定表」へ到達。48件の履歴、フォームの保存／取消、再読込、保存一覧から再開、書き出し／別コンテキストへの取り込み、直リンク、未知URL、保存不在を確認。初期HTMLがゲーム画面を含まないシェルであることも検査。
- Chrome headless / Playwright、1280×900と390×844。ページタイトル、画面表示、操作、エラーoverlay不在、consoleのerror・warning 0、横はみ出しなしを確認。Browser plugin not availableのため既存Playwright検証を使用。スクリーンショットを確認済み。結果・画面証跡は作業環境の`/tmp/parent-start-dev`・`/tmp/parent-start-static`（一時ファイル）に保存。
- `bun run preview --port 4175`でもホーム・未知URL・保存不在の案内を確認し、consoleエラーなし。

検証中に未知URLを静的シェルから開く際のhydration不一致を発見し、`ssr: false`のcatch-allルートを追加して解消。開発・静的配信の両方で修正後の全操作を再検証した。認証・購入検査・リクエストごとの部分SSRは[Q-017](open-questions.md#q-017ログイン購入検査と部分ssr)で後続仕様として管理する。Safari・Firefox、公開環境、人による面白さの評価は今回未実施。

## コミット前の検査（2026-09-15）

[S-015](specs/content.md#コマンドコミット時の検査)に従い、`lint`・`check`に生成スキーマ同期、JSON本体、コンテンツ整合性・画像参照の検査を統合した。Lefthook 2.1.14を固定し、pre-commitでステージ済みファイルのformat・再ステージ→lintを順に実行する。lintは警告も失敗扱いとする。`bun install --frozen-lockfile`のprepareでフックを自動設定でき、現在のリポジトリでも有効化済み。

検証：

- `bun run check`・`bun run lint`成功。現在のJSON本体4ファイルを診断し、スキーマ同期と独自整合性検査も実行。
- `bun run test -- tests/json-check-command.test.ts tests/json-schema.test.ts`：5テスト成功。追加JSONの自動検出、型違反・構文違反・末尾カンマの検出、日本語・空白を含むルート、行列付きの診断を確認。
- 一時リポジトリでインストール済みの`git hook run pre-commit`を実行。日本語・空白入りのファイル名の整形・再ステージ、部分ステージの未選択コメント保持、型違反JSON・古い生成スキーマ・lint警告によるコミット中止を確認。修正したステージ内容と作業ツリーを比較した。本作業用リポジトリのコミット・ステージ内容は検証用に変更していない。
- `bun run lefthook validate`・`bun run lefthook check-install`成功。フックは[lefthook.yml](../lefthook.yml)、JSON検査は[check-json.ts](../scripts/check-json.ts)。手順は[README](../README.md#コミット前の検査)。

これは開発コマンド・フックの技術検証。ゲーム規則・保存・描画は変更していない。

## 日本語の文章の見直し（2026-09-15）

D-028・[S-013 / AC-013-G](specs/gui.md#日本語の文章2026-09-15)に従い、本編のイベント・選択肢・観察・年代別の情景・結末、追加パックのサンプル、画面の操作名・説明・エラー案内を通読して改稿した。進路文の「探すへ」という接続、疲れの変化、生活費の調整などを自然な表現にした。第40期の出来事は表示時点の19歳6か月に合わせて「もうすぐ20歳」とした。

検証済み：

- `bun run check`成功。7ファイル47テスト成功。フォームの文言を参照する既存テスト・ブラウザ検証の期待値を更新した。
- `bun run test:public /tmp/parent-japanese-public-final 1`：7実行が全40期と成人後を完走し、5結末と全件の再生一致を確認。
- 改稿前の本編設定（Git HEAD）と改稿後の設定を共通エンジンへ渡し、3難易度×2家庭×3シードの18実行・720期で数値状態、親子の最終評価値、結末IDの一致を確認。公開プレイとは別の内部比較。比較スクリプトは作業環境の`/tmp/compare-japanese.ts`。
- `GAME_URL=http://127.0.0.1:5176 BROWSER_ARTIFACTS=/tmp/parent-japanese-browser bun run test:browser`成功。Chromeで40期・結末・48件の履歴・方針編集・再読込・再開・書き出し／取り込み・未知URL・保存不在を確認。1280×900と390×844で画面を確認し、横はみ出し・consoleのerror/warningなし。画面証跡は作業環境の`/tmp/parent-japanese-browser`。agent-browser未導入のため、既存のPlaywright検証を使用した。

設定に保存する改稿は新規開始から適用する。既存の保存に含まれる本文と旧data-1は保持する。履歴用テンプレートは旧文面へのフォールバックを持ち、既存保存の再生に対応する。人による読み心地の評価と公開デプロイは未実施。
